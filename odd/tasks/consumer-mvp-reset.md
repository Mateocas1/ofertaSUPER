# Consumer MVP reset

## Objective

Persist and execute the accepted four-milestone reset that turns ofertaSUPER from a portfolio/demo baseline into a small, trustworthy consumer MVP.

## Problem

The repository contains substantial UI, catalog, ingestion, safety, and operations work, but delivery is fragmented and the public experience still relies on a degraded demo catalog. Infrastructure and governance have outpaced visible consumer value.

## Why

A durable, versioned source of truth is required so future sessions resume the same sequence without reopening accepted product decisions or depending on conversation context.

## Product outcome

A person can search for a real product, compare an exact product across supermarkets, understand price freshness and availability, add products to a local basket, and compare basket totals.

## Accepted scope

- Initial supermarkets: Disco, Jumbo, and Carrefour.
- Exact cross-source product identity is established by a valid normalized matching EAN/GTIN.
- Pack, quantity, measurement unit, and variant are independently observed optional attributes: never infer them from names/descriptions or copy canonical target values into source observations.
- Missing optional attributes do not block EAN representation, freshness, or exact comparison; conflicting explicit attributes remain visible deterministic diagnostics.
- Initial catalog target: 500–1,000 useful products.
- At least daily refresh, subject to source capability.
- Anonymous public usage; basket remains local.
- Availability is a source observation, not guaranteed quantity or checkout inventory.
- Four sequential milestones: project reconciliation, small live catalog, real consumer journey, controlled public pilot.

## Constraints and non-goals

- Do not expand to six supermarkets before the initial three-source slice is credible.
- Do not introduce approximate product matching in the first MVP.
- Do not add consumer accounts, checkout, complex promotions, or advanced admin work.
- Local PostgreSQL is the acquisition and processing authority; a validated immutable static snapshot is the public Next.js/Vercel read surface.
- Supabase is temporary recovery/export only, will not be upgraded to Pro, and must not be deleted until backup/export parity is observed.
- Do not add another managed database.
- Pause new publication-authority, recovery, backup, audit, and operational layers unless the current consumer slice demonstrably requires them.
- Preserve existing branches and worktrees until reconciliation classifies them; do not delete ambiguous work.
- Use outcome gates rather than task-count completion.

## Delivery strategy

- ODD route: delegated direct for the multi-file documentation unit; later implementation routes are selected per task.
- Trigger evidence: the durable reset requires multiple non-trivial files and therefore uses one bounded writer.
- Delivery strategy: `ask-on-risk`.
- Forecast: under 400 authored lines for this documentation unit; later milestones must be forecast independently.
- Commit status: pending explicit user authorization; no commit is authorized by this document alone.
- TDD: not applicable to the documentation-only bootstrap. Future behavior tasks must resolve the repository's effective TDD mode and exact runner before implementation.

## Tasks

- [x] **CMVP-01 — Persist the roadmap.** Create the versioned consumer MVP roadmap, link its durable execution contract, and prevent local CodeGraph state from polluting diffs.
  - Route: delegated writer; multi-file write trigger.
  - Acceptance: one authoritative roadmap states milestones, gates, exclusions, resume protocol, and immediate next unit; `.codegraph/` is ignored without deleting local state.
  - Checks: structural readback, link/path consistency, `git diff --check`, and focused status inspection.
  - Evidence: `docs/consumer-mvp-roadmap.md` was structurally read back with the four milestones, outcome gates, exclusions, 12-unit order, authority/resume protocol, and CMVP-02 next step; README links it with a non-readiness claim; `.gitignore` contains `.codegraph/`; `git diff --check` passed; `git status --short` showed only the intended documentation/ignore changes plus pre-existing untracked `odd/`.

- [x] **CMVP-ORCH-01 — Bootstrap Herdr-assisted orchestration.** Establish the persistent parent/worker/verifier operating contract, blocker registry, and minimal safe Herdr automation before using it on CMVP-02.
  - Route: delegated writer; multi-file write and Herdr-specific exploration triggers.
  - Acceptance: versioned orchestration contract defines roles, dependency-aware concurrency, worktree isolation, blocker handling, stop conditions, and resume behavior; a blocker registry template exists; minimal automation can inspect/plan Herdr lanes without destructive or autonomous Git integration.
  - Checks: script syntax/help or dry-run, structural readback, `git diff --check`, and focused status inspection.
  - Evidence: `odd/orchestration/consumer-mvp-herdr.md` defines the parent/scout/writer/verifier/integrator boundary, three initial CMVP-02 read-only lanes, Herdr state caveats, isolation, envelopes, decision inbox, stop/recovery behavior, and prohibitions; `odd/blockers/consumer-mvp-blockers.md` provides the durable schema and records resolved authorization evidence; `scripts/orchestration/consumer-mvp-herdr.sh` completed syntax, help, doctor, and deterministic CMVP-02 dry-run checks without starting CMVP-02 or mutating Herdr/Git state.

- [x] **CMVP-02 — Reconcile the authoritative delivery line.** Inventory `origin/master`, OS03, active branches/worktrees, OpenSpec changes, and conflicting completion claims; select and prepare the authoritative feature line without deleting ambiguous work.
  - Route: Herdr-assisted parallel read-only scouts followed by one bounded reconciliation writer; no concurrent integration writers.
  - Acceptance: base branch, included work, paused work, preserved work, and exact integration sequence are explicit; baseline checks are observed.
  - Evidence: all three Herdr scouts completed; `docs/consumer-mvp-delivery-line-reconciliation.md` makes the base, included CMVP artifacts, paused/preserved boundaries, and exact integration sequence explicit. The user granted B-CMVP-001 authorization and the authorized `feat/consumer-mvp-reset` branch/worktree was prepared at `a62540b`, with `origin/master` as an ancestor. Baseline verification passed: `npm test` reported 99 pass/0 fail/12 skip; typecheck passed; lint had 0 errors and 195 pre-existing warnings; and shell-helper checks passed. Only intended CMVP artifacts were dirty. Native review `review-171abeb863d28cac` was approved and acknowledged; its informational cleanup observations were corrected without creating blockers. SEPA remains excluded and Vea remains incomplete.

- [x] **CMVP-03 — Establish the small live catalog.** Deliver recent, measurable Disco/Jumbo/Carrefour data with honest freshness and availability semantics.
  - Route: Herdr-assisted parallel read-only source/capability scouts, followed by dependency-ordered isolated writers and independent verification; no concurrent shared-database writes.
  - Acceptance: complete. The observed final catalog has 537 products, 728 offers, 728 history rows, 167 exact comparables, no `RUNNING` work, and an exactly 500-target frozen manifest. It satisfies three-source representation, ≥100 exact comparables, 100% freshness, and two consecutive successful cycles with the identical target fingerprint.
  - Evidence: complete. Discovery observed 36 calls, 1,157 unique products and 490 comparable products at count 50; operational count-25 evidence observed 618 unique products and 223 comparable products. Each of two sequential acquisition cycles recorded 36 checkpoints: 29 completed and 7 correctly fail-closed. Cycle 1 and cycle 2 passed with all sources, 167 comparables, and 100% freshness; both share fingerprint `1b7fe874958e9f6798ec76fa2e6115858c6bafcd785468bf357638cf52140fc0`, and cycle 2 reported `consecutiveSuccessful: true`. Focused gate, snapshot, and batch suites passed 5/5, 6/6, and 9/9; typecheck and lint passed after repair of verifier-caused `pnpm node_modules` displacement from local `.ignored` copies, without install, network, or source mutation. Native review `review-2de349146ad45d08` was approved and acknowledged. Four Herdr scouts mapped Disco, Jumbo, Carrefour, and measurement capability. At that initial mapping stage, they found breadth/freshness unproven and existing refresh writers unable to create missing coverage. **CMVP-03-T4a is complete:** valid normalized matching EAN/GTIN alone establishes exact cross-source identity. Pack, quantity, measurement unit, and variant remain independently observed optional attributes; missing values stay null and cannot block EAN representation, freshness, source representation, or comparison. Explicit conflicts remain visible deterministic diagnostics and are never canonicalized from the target. The read-only snapshot CLI accepts omitted optional-attribute evidence; the gate reports normalized-EAN/GTIN identity and diagnostic conflicts. Strict TDD RED used `./node_modules/.bin/tsx --conditions=react-server --test --test-name-pattern="CMVP catalog (gate|snapshot)" tests/cmvp-catalog-gate.test.ts tests/cmvp-catalog-snapshot.test.ts` and observed 4 pass/4 fail (8 total); GREEN used the same command and observed 8 pass/0 fail (8 total). Final `npm run typecheck` exited 0; `npm run lint -- --quiet` returned `[]`; and `git diff --check` exited 0. No acquisition, database schema/migration, Docker, network, Supabase, commit, push, merge, or T4b work was performed. **CMVP-03-T1 is complete:** `scripts/pipeline/cmvp-catalog-gate.ts` and `scripts/audit-cmvp-catalog-gate.ts` evaluate only caller-supplied frozen manifests and observed snapshots for exactly Disco, Jumbo, and Carrefour. The deterministic report separates exact EAN+pack+quantity+variant proof from EAN candidate overlap, records source representation, availability, price rankability, fixed 24-hour freshness, exclusions, cycle identity, and prior-cycle comparability; malformed, drifting, duplicate, unsupported, future, or invalid-price evidence fails closed. The independently confirmed identity-gating defect is corrected: only exact-identity-proven observations can satisfy source representation or freshness, while raw offer/availability/price diagnostics, exclusions, and EAN candidate overlap remain visible. Focused behavior tests passed (4/4). **CMVP-03-T2 is complete:** the bounded offline snapshot generator accepts an injected repository/data-access boundary and frozen target manifest, selects only Disco/Jumbo/Carrefour, and feeds the existing gate reporter without live DB or network execution. It totally orders generated offers by their complete serialized output fields, so equal `source:targetId` duplicates cannot inherit repository/DB return order; its corrected regression and final checks are recorded below. **CMVP-03-T3 is closed:** behavior-first source correction observed RED at 2 pass/1 fail because the Dockerfile lacked `FROM dependencies AS seeder`; GREEN passed 3/3 after adding the narrow seeder target that copies Prisma, runs `db:generate`, and changes only `seed.build.target`. Independent offline focused checks (3/3), typecheck, `lint --quiet`, and diff-check all passed. The authorized `npm run bootstrap:cmvp-local` succeeded with seed exit 0. A rerun through `docker compose up --build --abort-on-container-exit --exit-code-from seed seed` also succeeded with seed exit 0 without cleanup. Safe checks observed exactly three supermarkets—Carrefour, Disco, and Jumbo. In a read-only `SET LOCAL ROLE ofertasuper_app` transaction, counts were successfully selected from `categories`, `products`, `supermarkets`, `supermarket_products`, `price_history`, `promotions`, and `promotion_products`; effective app-role checks for schema CREATE, role administration, database creation, superuser, RLS bypass, and database CREATE privilege were all false. No objects or data were mutated by checks. An initial non-blocking attempt using role `postgres` failed because that role does not exist; subsequent configured-owner checks passed. B-CMVP-003 is resolved. Final resources: PostgreSQL healthy/running; role-provision, migrate, grants, and seed exited 0; network `ofertasuper-cmvp-local-bootstrap_default` is bridge with PostgreSQL attached; retained volume `ofertasuper-cmvp-local-bootstrap_postgres-data` is local. No cleanup/down/volumes/prune, Supabase, source probes, ingestion, T4, commit, push, review, or secret exposure occurred. Its planning acceptance boundary remains local-authority bootstrap plus reproducible fixture/seed-to-snapshot proof; its planning check boundary is focused proof, deterministic snapshot/gate readback, and no live-source, public-deployment, Supabase-deletion, or managed-database addition. B-CMVP-002 is mitigated and limited to temporary Supabase recovery/export connectivity: its two authorized strictly read-only attempts produced no DB access or results (first: `DATABASE_URL` absent; second: private existing environment, `PrismaClientInitializationError` without code), with no secrets exposed and no writes. It is not a prerequisite for T3 or CMVP-03 progress. This historical T3 evidence predates final CMVP-03 acceptance.

  - [x] **CMVP-03-T4b-1 — Align EAN/GTIN validation.** Introduce one shared pure normalizer/checksum validator and apply it consistently to VTEX normalization, ingestion quality gates, the CMVP gate, and snapshot generation. Normalize spaces/hyphens; accept only supported 8–14 digit GTIN lengths with valid check digits; fail closed for malformed, missing, checksum-invalid, and duplicate normalized identities; preserve valid behavior and never infer identity from names.
    - Status: complete and approved under native review lineage `review-710c21f80035b825`.
    - Route: delegated writer; multi-file write trigger.
    - TDD: strict, explicitly requested by the user; focused RED/GREEN runner must cover the existing VTEX, quality-gate, CMVP gate, and snapshot suites.
    - Evidence: RED `./node_modules/.bin/tsx --conditions=react-server --test tests/vtex.test.ts tests/ingestion-quality-gates.test.ts tests/cmvp-catalog-gate.test.ts tests/cmvp-catalog-snapshot.test.ts` observed 23 pass/6 fail (29 total): formatted valid GTINs were rejected, checksum-invalid VTEX and ingestion identities passed, and duplicate normalized source identities were not rejected. GREEN with the same command observed 29 pass/0 fail. `npm run typecheck` exited 0; `npm run lint -- --quiet` returned `[]`; and `git diff --check` exited 0. Independent verification repeated 29 pass/0 fail plus passing typecheck, quiet lint (`[]`), and diff-check, and found no forbidden T4b-1 scope expansion. The parent spot-check repeated the focused command at 29 pass/0 fail and `git diff --check` passed.
    - Constraints: no DB schema, ingestion orchestration, adapters, Docker, network, manifests, T4b-2+, commits, or native review.
  - [x] **CMVP-03-T4b-2 — Add bounded acquisition runner and artifact.** Reuse the existing source-scoped staged ingestion path for bounded acquisition while recording enough deterministic artifact evidence to resume safely; do not use legacy scraper writers or direct-refresh paths that cannot create initial coverage.
    - Status: complete; implementation and independent offline verification passed. Native review was intentionally not started.
    - Route: delegated writer; preparation, four-file mapping, and multi-file write triggers.
    - TDD: strict RED→GREEN, explicitly requested by the user; offline focused tests with injected adapter/persistence fakes, followed by independent verification, typecheck, quiet lint, and diff-check.
    - Constraints: exactly one of Disco/Jumbo/Carrefour per invocation, first-batch count exactly 5, one explicit nonblank term, no writes in dry-run, explicit confirmation plus exactly five distinct valid normalized expected GTINs for writes, mismatch before reconciliation, deterministic immutable artifact and replay/conflict semantics, sequential partial-failure/resume state; no network, Docker, actual DB execution, schema/migration, Supabase, manifest freeze, T4b-3+, commit, push, or native review.
    - Evidence: strict offline RED `./node_modules/.bin/tsx --conditions=react-server --test tests/cmvp-first-batch.test.ts` observed 0 pass/1 fail (the new pure orchestration module was missing). GREEN with the same command observed 4 pass/0 fail. The injected-fake suite covers source/term/count/GTIN validation, dry-run no-write behavior, write confirmation, fetched/admitted mismatch before reconciliation, deterministic sorted artifact fields, completed checkpoint replay, changed-contract conflict, and blocked-state resume. Final required checks passed: `./node_modules/.bin/tsx --conditions=react-server --test tests/cmvp-first-batch.test.ts tests/ingestion/**/*.test.ts` 4 pass/0 fail; `npm run typecheck` exited 0; `npm run lint -- --quiet` returned `[]`; and `git diff --check` exited 0. The task remains in progress pending parent independent verification. **Independent-verifier correction:** strict RED `./node_modules/.bin/tsx --conditions=react-server --test tests/cmvp-first-batch.test.ts` observed 3 pass/1 fail because a completed dry-run checkpoint replayed as a confirmed write; GREEN with the same command observed 4 pass/0 fail after the contract digest included the `dry-run`/`confirmed-write` execution mode. CLI contract: `npm run acquire:cmvp-first-batch -- --batch-id=<id> --source=<disco|jumbo|carrefour> --term=<nonblank> --count=5 --expected-gtins=<five comma-separated valid GTINs> --dry-run --output=<new checkpoint path>`; omit `--dry-run` only with `--confirm-write`. Each write saves an immutable artifact beneath the checkpoint's `.artifacts/<contract-digest>/` directory while updating only the checkpoint pointer for resume. Runtime execution remains separately unauthorized and requires the existing local staged-ingestion database, configured source adapter credentials, and a new checkpoint path. Independent re-verification passed 4/4 focused tests, typecheck, quiet lint (`[]`), and diff-check, finding no remaining severe or bounded defect. The parent spot-check repeated the focused suite at 4/4 and `git diff --check` passed.
  - [x] **CMVP-03-T4b-3 — Execute the sequential first batch.** Run bounded acquisition sequentially for Disco, Jumbo, and Carrefour against local PostgreSQL, respecting existing source-scoped active-write and reconciliation lock boundaries and capturing per-source outcomes.
    - Status: complete. The reviewed public-search fallback and fail-closed acquisition checkpoint corrections passed focused tests, typecheck, quiet lint, diff-check, and native review `review-a9b15731acc3f097` (approved and acknowledged). The authorized sequential local batch then completed Disco, Jumbo, and Carrefour with exactly five fetched/admitted GTINs per source and zero rejections.
    - Authorization boundary: use the approved T4b-2 CLI and existing source-adapter preview capability; load only `VTEX_SHA256_HASH` from the named private local environment file into each required runtime process without exposing or persisting it; preserve the already verified local PostgreSQL app-role environment, volume, and containers. For each source, obtain exactly five distinct valid normalized GTIN candidates with the smallest bounded public query/preview, run a new-path dry-run, inspect exact-set equality, and only then run a confirmed write with a separate new write checkpoint. Stop that source on mismatch/error; continue an independent later source only when safe. Capture redacted run IDs, counts, timestamps, admitted GTINs, errors, artifact paths/digests, and prove idempotency by one identical confirmed replay only when needed or by read-only database checks.
    - Evidence: `2026-09-19T18:07:01Z` private local PostgreSQL checks observed a healthy accepting service and successful read-only catalog access under `ofertasuper_app`. The bounded Disco source-adapter preview (one term, `count: 5`) failed before any public request with `VTEX_SHA256_HASH is required`, despite loading the approved local runtime environment. On `2026-09-19T18:10:47Z`, after exact reauthorization and before acquisition, static inspection found the approved CLI imports `scripts/load-env`, whose project environment loading can load `DATABASE_URL` or `DIRECT_URL` from the named private file. That conflicts with the authorization to load only `VTEX_SHA256_HASH`; this is a real safety blocker before the first source preview. At `2026-09-19T20:35:24Z`, the approved environment-isolation correction was confirmed by static inspection: the corrected CLI has no environment-file loader import or invocation. The local PostgreSQL container is healthy but has no host-exposed port, and no already-running CLI-capable local process with the required explicit app-role connection was available. Creating one would be an unapproved Docker runtime/lifecycle action, so execution stopped before private runtime processing, source previews, or writes. No private environment file, candidates, dry-run/write checkpoints, run IDs, source payloads, database writes, Docker lifecycle actions, public requests, or credential values were produced/exposed. Redacted evidence: `artifacts/cmvp/first-batch/2026-09-19T20-35-24Z/runtime-connectivity-blocker.json` (SHA-256 `a1537671acbc51b28cd38c73ef08f02a65765fd01cba0f5c5927d3612f132403`). The approved CLI was not bypassed; see `B-CMVP-004`.
    - Prohibited: Supabase, deployment, scheduler, all-source execution, cleanup, destructive Docker actions, source-code edits, migrations, schema changes, Docker rebuild/down/volume cleanup, manifest freeze, T4b-4, Git commit/push/merge, and native review.
    - Retry authorization and outcome: at `2026-09-19T20:53:21Z`, the user explicitly authorized exactly one diagnosable Disco retry through `getSourceAdapter("disco").fetchProducts(["leche"], {count:5})`, using only a temporary network-attached read-only runtime bridge and a sanitizing wrapper. It privately loaded only `VTEX_SHA256_HASH`, used explicit local app-role `DATABASE_URL` with `DIRECT_URL` unset, and wrote only fresh redacted first-batch evidence. The one public request started with count 5, then exited 1 with sanitized `VtexRequestError` / `adapter_failure`; no GTINs were admitted. `artifacts/cmvp/first-batch/2026-09-19T20-55-10Z/disco/disco-diagnostic.json` SHA-256 `b9018b9ee2ea9131d0d1831c32c074c8e410f043de536a9fe6ea4762d42d4924`; container removal receipt `artifacts/cmvp/first-batch/2026-09-19T20-55-10Z/disco/container-removal.json` SHA-256 `a237be0cb38ffbd996920438ed7b6f0233e8c42e08e61cf336e7715f4c3bc905`.
    - Completion evidence: the final Disco retry and confirmed write are under `artifacts/cmvp/first-batch/20260920T072329Z/disco/`; Jumbo and Carrefour evidence is under `artifacts/cmvp/first-batch/20260920T072456Z/{jumbo,carrefour}/`. Independent read-only verification confirmed ingestion runs 1–3 are `SUCCESS`, each with fetched/staged/rejected metrics `5/5/0`, and each created 5 products, 5 offers, and 5 price-history rows. No associated run remains `RUNNING`. The 15 admissions contain 11 unique GTINs and four cross-source overlaps, proving exact cross-source EAN coverage in this first bounded batch.
  - [x] **CMVP-03-T4b-4 — Freeze manifest and snapshot proof.** Generate the deterministic target manifest and immutable snapshot/gate evidence needed to measure breadth, comparison coverage, source representation, freshness, and repeatability.
    - Status: complete for the bounded first-batch baseline. The snapshot CLI contract defect was fixed with strict RED→GREEN coverage so it emits only the generated snapshot; gate evaluation remains a separate CLI.
    - Evidence: `artifacts/cmvp/catalog/first-batch-20260920/target-manifest.json` freezes 11 sorted valid GTIN targets; `snapshot.json` contains 15 deterministic offers across Disco, Jumbo, and Carrefour and reproduced byte-identically; `gate-report.json` records 100% freshness, all three sources, four exact comparable GTINs, zero exclusions, and zero attribute conflicts. Its expected `FAIL` is limited to scale: target size 11/500 and exact comparable products 4/100. Independent verification passed 6/6 focused tests, typecheck, quiet lint, and diff-check and reproduced target fingerprint `d74023488b0525e073a48257116a58a47cdc13810e05fbcdd7385f3439d719dd` exactly.
  - [x] **CMVP-03-T5 — Expand to the catalog acceptance gate.** Preserve the frozen 11-GTIN baseline, add a bounded resumable catalog-batch contract, discover and freeze exactly 500 useful GTINs with a 125-comparable safety margin, acquire sequential source/term batches capped at 25 admitted GTINs, and prove ≥100 exact comparable GTINs, all three sources, ≥90% freshness, and two consecutive successful cycles over the identical target fingerprint.
    - Status: complete.
    - Route: one bounded delegated writer with serialized edits, followed by an independent verifier; operational public-source and PostgreSQL batches remained sequential and checkpointed.
    - Evidence: discovery used 36 calls and produced 1,157 unique / 490 comparable products at count 50. Operational count-25 evidence produced 618 unique / 223 comparable products. Two sequential cycles each recorded 36 checkpoints, with 29 completed and 7 correctly fail-closed. The final database has 537 products, 728 offers, 728 history rows, 167 exact comparables, and no `RUNNING` work; the frozen target manifest contains exactly 500 targets. Both cycles passed at 100% freshness across all sources with 167 comparables and identical fingerprint `1b7fe874958e9f6798ec76fa2e6115858c6bafcd785468bf357638cf52140fc0`; cycle 2 reported `consecutiveSuccessful: true`. The seven fail-closed batch outcomes are informational successful safety behavior, not an unresolved blocker.
    - Constraints: no mutation of `artifacts/cmvp/catalog/first-batch-20260920/`; no schema migration, Supabase, scheduler, deployment, destructive Docker/Git action, approximate matching, or concurrent database writers. New cycle evidence is append-only under `artifacts/cmvp/catalog/<cycle-id>/`.
    - Checks: focused gate 5/5, snapshot 6/6, and batch 9/9 passed. Typecheck and lint passed after repairing verifier-caused `pnpm node_modules` displacement from local `.ignored` copies, without install, network, or source mutation. Native review `review-2de349146ad45d08` was approved and acknowledged.

- [ ] **CMVP-04 — Complete the real consumer journey.** Replace illustrative public surfaces with live catalog data and verify search → compare → freshness/availability → basket totals.
  - Route: to be derived after CMVP-03.
  - Acceptance: no illustrative data presented as real; five-product basket totals and coverage are correct; mobile journey and degraded states are verified in production.
  - Evidence: pending.

- [ ] **CMVP-05 — Run a controlled public pilot.** Observe 5–15 external users and classify failures as data, trust, or interaction problems before expanding scope.
  - Route: to be derived after CMVP-04.
  - Acceptance: at least five users complete the journey; search success, comparison coverage, basket use, abandonment, and trust feedback are recorded; the next product investment is decided from evidence.
  - Evidence: pending.

## Progress

- The user accepted the four-milestone consumer-first reset exactly as proposed.
- The project audit established that a portfolio/demo MVP exists but a broad, current live catalog does not.
- A tooling incident created untracked `.codegraph/` local index state; it was diagnosed as generated state and will be ignored, not deleted.
- CMVP-01 is complete: the roadmap is versioned, discoverable from README without a live-readiness claim, and local `.codegraph/` state is ignored without deletion.
- The user accepted the Herdr-assisted parent/worker/verifier factory with dependency-aware concurrency and durable blocker classification.
- CMVP-ORCH-01 is complete; the three CMVP-02 Herdr scouts are complete and the reconciliation recommendation is recorded.
- CMVP-02 is complete: authorization was granted, the authorized `feat/consumer-mvp-reset` branch/worktree was prepared at `a62540b`, its documented reconciliation acceptance is explicit, and baseline verification passed. Native review `review-171abeb863d28cac` is approved and acknowledged; its informational cleanup observations were resolved without becoming blockers.
- CMVP-03 is complete. CMVP-03-T4b-1 is approved under native review lineage `review-710c21f80035b825`, and CMVP-03-T4b-2 is complete with strict offline TDD, independent verification, and a parent spot-check. CMVP-03-T4a is complete under the accepted EAN-only exact-identity policy; the durable acceptance and strict RED→GREEN runner are recorded above. Local PostgreSQL is its acquisition and processing authority, and a validated immutable static snapshot is the public Next.js/Vercel read surface. CMVP-03-T1 provides the fixture/snapshot-backed read-only gate reporter. CMVP-03-T2 is complete: its injected-boundary offline snapshot generator has a regression proving byte-equivalent output for equal-key duplicate offers returned in opposite orders, and it uses a complete serialized-offer ordering independent of repository/DB return order. CMVP-03-T3 is closed. Behavior-first source correction observed RED at 2 pass/1 fail because the Dockerfile lacked `FROM dependencies AS seeder`; GREEN passed 3/3 after adding the narrow seeder target that copies Prisma, runs `db:generate`, and changes only `seed.build.target`. Focused offline checks (3/3), typecheck, `lint --quiet`, and diff-check passed. `npm run bootstrap:cmvp-local` succeeded with seed exit 0; an existing-seed rerun via `docker compose up --build --abort-on-container-exit --exit-code-from seed seed` also exited 0 without cleanup. Exactly Carrefour, Disco, and Jumbo were observed. A read-only `SET LOCAL ROLE ofertasuper_app` transaction selected counts from the required catalog tables, while effective app-role checks for schema CREATE, role administration, database creation, superuser, RLS bypass, and database CREATE privilege were all false. The initial `postgres`-role check failure was non-blocking because that role does not exist; configured-owner checks then passed. B-CMVP-003 is resolved. PostgreSQL remains healthy/running; role-provision, migrate, grants, and seed exited 0; the bridge network and retained local PostgreSQL-data volume remain. No cleanup/down/volumes/prune, Supabase, source probes, ingestion, T4, commit, review, push, or secret exposure occurred. B-CMVP-002 is mitigated: Supabase remains temporary recovery/export only and its connectivity is not a CMVP-03 prerequisite. Do not upgrade Supabase to Pro or delete it before backup/export parity is observed; do not add another managed database. At that bootstrap stage, no live snapshot had been authorized or measured and no catalog write had started. SEPA remains excluded and Vea remains incomplete.

## Verification evidence

- Public demo inspection showed a four-product illustrative home and degraded stale demo search data.
- Repository audit found extensive real acquisition and guarded-write capabilities, but no proof of current broad freshness or recurring operation.
- Git/worktree audit found no single unambiguous current delivery line.
- CMVP-01 structural readback confirmed the roadmap, README link, `.gitignore` entry, and ledger update; `git diff --check` passed.
- CMVP-ORCH-01 observed checks: `bash -n scripts/orchestration/consumer-mvp-herdr.sh` exited 0; `bash scripts/orchestration/consumer-mvp-herdr.sh help` exited 0 and rendered command/safety guidance; `bash scripts/orchestration/consumer-mvp-herdr.sh doctor` exited 0 with Git worktree and `herdr 0.9.0` prerequisites; `bash scripts/orchestration/consumer-mvp-herdr.sh plan CMVP-02` exited 0 and rendered the three-scout dry-run plan; `git diff --check` exited 0; `git status --short` showed pre-existing modified `.gitignore` and `README.md`, pre-existing untracked `docs/consumer-mvp-roadmap.md` and `odd/`, plus untracked `scripts/orchestration/`.
- CMVP-03-T1 observed checks: the identity-gating regression first failed with Carrefour's 51 malformed offers incorrectly reporting `represented: true` and `withinWindow: 51`; after the bounded correction, direct focused execution `./node_modules/.bin/tsx --conditions=react-server --test --test-name-pattern="CMVP catalog gate" tests/cmvp-catalog-gate.test.ts` passed 4/4. `npm run typecheck`, `npm run lint -- --quiet`, and `git diff --check` exited 0; `git status --short` was inspected. The earlier npm-runner argument-order caveat remains recorded by repository history. No DB, network, secret, write, scheduler, source-adapter, migration, publication, delivery, or native-review action was performed.
- CMVP-03-T2 initial TDD evidence: RED was observed with `./node_modules/.bin/tsx --conditions=react-server --test --test-name-pattern="CMVP catalog snapshot" tests/cmvp-catalog-snapshot.test.ts`: it failed because `../scripts/audit-cmvp-catalog-snapshot` did not exist. GREEN was then observed with the same command: 2/2 tests passed. The tests prove an injected boundary with no write methods, strict Disco/Jumbo/Carrefour selection, frozen useful-target restriction, deterministic sorted output, and null pack/quantity/variant as unproven identity. `npm run typecheck` first exposed nullable observation and target-EAN type errors, then passed after the bounded null filtering/type refinement; `npm run lint -- --quiet` returned `[]`; `git diff --check` exited 0.
- CMVP-03-T2 correction TDD evidence: RED was observed with `./node_modules/.bin/tsx --conditions=react-server --test --test-name-pattern="CMVP catalog snapshot" tests/cmvp-catalog-snapshot.test.ts`: the new equal `source:targetId` duplicate-offer regression failed deep equality when the same two rows were returned in opposite orders. GREEN was then observed with the same command: 3/3 tests passed after complete serialized-offer ordering was added. Final verification passed: `npm run typecheck` exited 0; `npm run lint -- --quiet` returned `[]`; `git diff --check` exited 0; and `git status --short` was inspected. No live DB or network attempt, secrets, writes, migrations, dependencies, scheduler, publication, delivery, commit, or review action was performed.
- **CMVP-03-T4a strict TDD evidence:** RED used `./node_modules/.bin/tsx --conditions=react-server --test --test-name-pattern="CMVP catalog (gate|snapshot)" tests/cmvp-catalog-gate.test.ts tests/cmvp-catalog-snapshot.test.ts` and observed 8 total tests: 4 pass/4 fail. The failures proved the former EAN+pack+quantity+variant gate rejected null optional attributes, did not normalize EANs, and did not preserve observed optional attributes/conflicts. GREEN used the same exact command and observed 8 total tests: 8 pass/0 fail after the bounded EAN-only implementation. Final `npm run typecheck` exited 0; `npm run lint -- --quiet` returned `[]`; and `git diff --check` exited 0. Independent verification then observed the same focused 8 pass/0 fail result, passing typecheck, quiet lint output `[]`, and a clean `git diff --check`; the parent spot-check independently reran the focused command at 8 pass/0 fail. No acquisition, DB/schema/migration, Docker, network, Supabase, commit, push, merge, or review operation occurred; T4b remains out of scope.
- CMVP-03-T3 runtime evidence: the initial bootstrap/migration recovery remains recorded above. **B-CMVP-003 source correction and closure:** behavior-first focused RED observed 2 pass/1 fail because the Dockerfile lacked `FROM dependencies AS seeder`; GREEN passed 3/3 after adding a narrow seeder target that copies Prisma, runs `db:generate`, and switches only `seed.build.target`. Independent offline focused checks (3/3), typecheck, `lint --quiet`, and diff-check passed. The authorized `npm run bootstrap:cmvp-local` succeeded with seed exit 0. An existing seed service rerun via `docker compose up --build --abort-on-container-exit --exit-code-from seed seed` also succeeded with exit 0 without cleanup. Safe checks observed exactly three supermarkets: Carrefour, Disco, and Jumbo. In a read-only transaction using `SET LOCAL ROLE ofertasuper_app`, counts were successfully selected from `categories`, `products`, `supermarkets`, `supermarket_products`, `price_history`, `promotions`, and `promotion_products`. Effective app-role checks were all false for schema CREATE, role administration, database creation, superuser, RLS bypass, and database CREATE privilege. No objects or data were mutated by checks. The initial role `postgres` check failed because that role does not exist and was non-blocking; subsequent configured-owner checks passed. Final status: PostgreSQL healthy/running; role-provision, migrate, grants, and seed exited 0; network `ofertasuper-cmvp-local-bootstrap_default` is bridge with PostgreSQL attached; volume `ofertasuper-cmvp-local-bootstrap_postgres-data` is local and retained. No cleanup/down/volumes/prune, Supabase, source probes, ingestion, T4, commit, push, review, or secret exposure occurred. B-CMVP-003 is resolved; this historical evidence predates the completed CMVP-03-T5 acceptance gate.
- CMVP-03 final acceptance is complete: discovery recorded 36 calls, 1,157 unique / 490 comparable products at count 50; count-25 operations recorded 618 unique / 223 comparable products. Two sequential cycles each had 36 checkpoints with 29 completed and 7 correctly fail-closed. The final database has 537 products, 728 offers, 728 history rows, 167 exact comparables, no `RUNNING` work, and a frozen manifest of exactly 500 targets. Both cycles passed with 100% freshness, all sources, 167 comparables, and fingerprint `1b7fe874958e9f6798ec76fa2e6115858c6bafcd785468bf357638cf52140fc0`; cycle 2 is consecutively successful. Focused gate/snapshot/batch checks passed 5/5, 6/6, and 9/9; typecheck/lint passed after local `.ignored` `pnpm node_modules` repair with no install, network, or source mutation. Native review `review-2de349146ad45d08` was approved and acknowledged.

## Resume protocol

1. Read this file.
2. Load Engram topic `odd/consumer-mvp-reset/tasks` and reconcile both copies.
3. Inspect current Git status and the latest evidence for the first unchecked task.
4. Continue only that task; do not skip milestone gates or reopen accepted scope without new user authorization.
5. Update this file, its Engram mirror, and the visible todo projection after every task transition or material plan change.

## Next step

CMVP-04 — Complete the real consumer journey. Do not start it until separately authorized.
